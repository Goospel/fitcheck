# CLAUDE.md — FitCheck

이 레포에서 코드를 쓸 때 지켜야 할 규칙. **매 세션 자동으로 읽히므로 여기 적힌 것이 기본값이다.**

백엔드 3명이 각자 클로드를 돌려 병렬로 작업한다. 누가 뭘 맡는지는 [plan.md](plan.md)에 있다. 이 문서는 **어떻게 쓰는가**만 다룬다.

서비스가 무엇인지는 [PRD](https://www.notion.so/PRD-AI-FitCheck-3b722a79abb58106a343cf9c92b82e61) · [화면 설계서](https://www.notion.so/AI-FitCheck-3b722a79abb581498813d975f9ddc0ca).

---

## 1. 절대 지어내지 말 것 — 확정 상수

⚠️ **이 절이 이 문서의 존재 이유다.** 아래 값들은 PRD에서 확정된 것이고, 구현 중 임의 조정이 금지돼 있다. 값이 어긋나면 핏 리포트·이미지 프롬프트·사이즈 비교 화면이 **한꺼번에** 깨진다.

모르는 값이 나오면 **추론하지 말고 물어본다.** 그럴듯한 숫자를 채워 넣는 것이 이 프로젝트에서 가장 비싼 실수다.

### 핏 등급 임계값 (가슴 여유량 기준, cm)

```
          0        6        14       24
  --------|--------|--------|--------|------>
  너무 작음  슬림    레귤러   세미오버   오버
```

**하한 포함, 상한 미포함.** 여유 6cm면 레귤러핏, 14cm면 세미오버핏, 24cm면 오버핏.

⚠️ 0cm 미만의 등급명은 **「너무 작음」**이다 (PRD 6.2.2). 「타이트핏」이 아니다 — 핏 종류가 아니라 경고다.
**선호 핏으로 고를 수 있는 건 나머지 4개뿐이다** (PRD 7.2) — 「너무 작음」을 선호할 사람은 없다.

### 신축성 보정

| 입력 | 처리 |
|---|---|
| 좋음 | 각 구간 하한을 **6cm 낮춤** |
| 약간 | 각 구간 하한을 **4cm 낮춤** |
| 없음 | 보정 없음 |
| 미입력 | **"없음"으로 간주** — 보수적 판정. 큰 옷을 사게 하는 편이 작은 옷보다 낫다 |

### 여유량 계산식

| 부위 | 식 |
|---|---|
| 가슴 여유 | (의류 가슴단면 × 2) − 사용자 가슴둘레 |
| 허리 여유 | (의류 허리단면 × 2) − 사용자 허리둘레 · **입력된 경우만** |
| 어깨 차이 | 의류 어깨너비 − 사용자 어깨너비 |
| 소매 길이 차 | 의류 소매길이 − 사용자 팔길이 |

### 기장 5단계 — 이 문구 그대로

```
허리선 위      "허리 위로 올라오는 크롭 기장"
허리선        "허리에 딱 떨어지는 기장"
골반          "골반에 걸치는 기본 기장"
엉덩이 중간    "엉덩이를 반쯤 덮는 기장"
엉덩이 아래    "엉덩이를 완전히 덮는 롱 기장"
```

문구는 `fit/length.py` 의 `LENGTH_LABELS` 에 있다 — **새로 타이핑하지 말고 import 한다.** 어느 문구가 나올지는 총장과 사용자 키로 `length_label()` 이 정한다 (Q1 종결).

### 소매 3단계

```
손목 위 / 손목 / 손등 일부 덮음
```

"골반 아래" 처럼 목록에 없는 표현을 만들지 않는다. 분기 개수가 여기서 확정된다.

### 드롭숄더 임계값 (이미지 프롬프트 전용)

```
어깨 diff(의류 어깨너비 − 사용자 어깨너비) >= 4cm  →  드롭숄더 지시
```

⚠️ **3명 동의 대기 중 (PR 확인 필요).** Q4(`docs/open-questions.md`) 종결안으로 `images/prompt.py`에는 이미 반영했지만, 이 표에 올리는 것 자체는 확정 상수라 셀프 머지 대상이 아니다. 승인 전까지는 「코드에는 있지만 팀 전체 확정은 아닌 값」으로 본다.

### 그 밖의 고정값

| 항목 | 값 |
|---|---|
| 지원 의류 | 티셔츠 / 셔츠 / 니트 / 후디 / 맨투맨 — **상의 5종만** |
| 성별 | 남성 / 여성 / 밝히지 않음 — 3지선다. 치수 추정 용도로만 사용 |
| 키 입력 범위 | 100 ~ 220 (정수, cm) |
| 몸무게 입력 범위 | 30 ~ 200 (정수, kg) |
| 프로필 정확도 | `n/5` — 실측 4개(어깨·가슴·허리·팔) + 선호 핏 1개 |
| 사진 최소 해상도 | 512 × 768 |
| 이미지 생성 타임아웃 | 10분 |
| 핏 리포트 계산 | 100ms 이내 |

### ⛔ 여기 없는 값은 아직 확정된 게 아니다

위 표에 없다고 해서 "자유롭게 정해도 되는 값"이 아니다. **아직 팀이 안 정한 공백**이다.

| 미확정 | 상세 |
|---|---|
| 소매 3단계 판정 기준 (경계 2개) | [Q2](docs/open-questions.md) |

✅ **A4 신체 치수 추정 계수는 확정됐다** (Q3 종결) — 사이즈코리아 8차 조사 앵커. `fit/estimate.py` 의 `ANCHORS` 가 유일한 출처고 **출처 없이 고치지 않는다.**

✅ **기장 5단계 경계도 확정됐다** (Q1 종결) — 같은 조사의 **랜드마크 높이**다. `fit/length.py` 의 `LANDMARKS` 가 유일한 출처고 **출처 없이 고치지 않는다.** 경계는 `목뒤높이 − 랜드마크높이` 를 키 비율로 늘려 그때그때 계산한다 — 표에 박힌 숫자가 아니다.

✅ **드롭숄더 임계값도 확정됐다** (Q4 종결) — PRD 6.2.4 예시값 4cm. `images/prompt.py` 의 `SHOULDER_DROP_THRESHOLD` 가 유일한 출처다.

이 중 하나가 필요해지면 **구현을 멈추고 [docs/open-questions.md](docs/open-questions.md)를 읽은 뒤 사용자에게 묻는다.** 임시값을 넣고 진행하지 않는다.

핏 등급 문구 5종은 확정이고 `fit/grade.py` 에 있다 — **새로 타이핑하지 말고 `from fit.grade import GRADE_ORDER, PREFERRED_GRADES` 로 가져온다.**

---

## 2. 기술 스택 — 정해진 것을 정해진 대로 쓴다

스택은 확정됐다. 목록과 선정 이유는 [plan.md 2절](plan.md). 여기는 **코드에서 지킬 규약**만.

| | |
|---|---|
| 실행 | `uv run ...` — `pip install` 금지, 가상환경을 직접 만들지 않는다 |
| 요청·응답 모델 | **전부 `core.schema.Schema` 를 상속한다.** camelCase 변환이 여기 걸려 있다 |
| 에러 | **전부 `core.errors.AppError` 를 올린다.** 직접 `JSONResponse` 로 에러를 만들지 않는다 |
| DB 접근 | SQLAlchemy 2.0 **async** 세션. 동기 세션·raw psycopg를 섞지 않는다 |
| 스키마 | `db/models.py` 가 유일한 출처. **BE-2만 고친다.** 마이그레이션 도구는 안 쓴다 |
| 잡 큐 | `image_job` 테이블 + `FOR UPDATE SKIP LOCKED`. **Redis·Celery·RQ 추가 금지** |
| 이미지 모델 호출 | `images/` 안 **한 곳에서만.** 다른 데서 이미지 생성 API를 직접 부르지 않는다 |
| 라우터 등록 | `main.py` 의 주석 블록에 **자기 줄만** 추가한다 |

⚠️ **DB는 팀 공용 1개다. 내 로컬 DB가 아니다.** `drop_all()` · `TRUNCATE` · 테스트에서 실 DB에 쓰기 — 셋 다 남의 작업을 지운다. 테스트는 순수 함수만 대상으로 한다.

⚠️ **10분짜리 이미지 잡 때문에 서버리스로 갈 수 없다.** Vercel·Lambda 계열로 옮기자는 제안을 하지 않는다.

---

## 3. 디렉터리 소유권 — 남의 영역을 건드리지 않는다

세 개의 클로드가 동시에 돈다. 같은 파일을 만지면 머지 충돌로 시간을 다 쓴다.

| 슬롯 | 소유 디렉터리 |
|---|---|
| BE-1 | `fit/` · `api/garments` · `api/fittings` |
| BE-2 | `db/` · `auth/` · `api/profile` · `core/` |
| BE-3 | `images/` · `jobs/` · `api/history` · `api/results` |

⚠️ **BE-2 슬롯은 2026-08-19부터 BE-1(KimZion)이 겸한다** — 원 담당이 하루 동안 착수하지 않아 스키마가 나머지 둘을 막고 있었다. `db/models.py` 와 `db/session.py` 는 이미 `main` 에 있으니 **다시 만들지 말고** 팀에 말한 뒤 이어받는다. 상세는 [plan.md 1절](plan.md).

**남의 디렉터리에 수정이 필요하면 직접 고치지 말고 담당자에게 말한다.**

커밋 전에 반드시 확인한다:

```bash
git diff --stat
```

내 디렉터리 밖의 파일이 보이면 **왜 바뀌었는지 설명할 수 있어야 한다.** 설명이 안 되면 되돌린다. 요청하지 않은 파일까지 손대는 것이 이 방식의 대표적 사고다.

---

## 4. 공용 파일 — 혼자 고치지 않는다

아래는 세 명 모두가 의존하는 파일이다. 동시에 고치면 충돌이 나고, 조용히 고치면 남의 코드가 깨진다. **고치기 전에 팀에 알린다.**

| 파일 | 규칙 |
|---|---|
| `docs/contracts.md` | 세션 시작 시 **먼저 읽는다.** 변경은 팀 합의 후 |
| `db/models.py` (DB 스키마) | **BE-2만 고친다.** 팀 공용 DB라 잘못 바꾸면 셋이 같이 멈춘다 |
| `pyproject.toml` · `uv.lock` | 의존성 추가 전에 알린다. lock 충돌은 병합하지 말고 `uv lock` 으로 다시 만든다 |
| `main.py` | 라우터 등록 줄 **자기 것만** 추가한다. 남의 줄·미들웨어 순서를 건드리지 않는다 |
| `.env.example` | 환경변수를 추가하면 **여기도 같이 갱신하고 팀에 공유한다.** 안 그러면 남의 로컬이 안 뜬다 |
| `core/` (에러 규격 · camelCase 베이스 · 설정) | BE-2가 소유한다. 여기 모양을 바꾸면 세 명의 API가 동시에 바뀐다 |
| `CLAUDE.md` · `plan.md` | 내 담당 체크박스는 자유롭게 ✅. 규칙 변경은 합의 후 |

---

## 5. Git

- **`main`에서 직접 작업하지 않는다.** 브랜치는 **최신 `origin/main` 기준**으로 딴다

  ```bash
  git fetch origin && git checkout -b feat/be1-a5-report origin/main
  #                                        ^^^ 슬롯   ^^ 작업 단위 ID (plan.md)
  ```

  **이름에 슬롯과 작업 단위 ID를 넣는다.** 셋이 같은 origin에 푸시하므로 `feat/api` 같은 이름은 충돌하고, `git branch -r` 만 봐도 누가 뭘 하는지 보여야 한다.
- 브랜치 → PR → 머지. `main`에 직접 push 하지 않는다 (브랜치 보호로 막혀 있다 — force push·삭제도 관리자 포함 차단)
- 머지는 **squash + 브랜치 삭제**. `gh pr merge <번호> --squash --delete-branch`
- 머지 후 로컬 정리: `git checkout main && git pull && git fetch --prune`
- 커밋 메시지·PR 본문은 한국어. **무엇을 왜** 바꿨는지만. 코드 세부는 diff에 있다

### 머지 규칙 — 승인 여부는 영향 범위로 가른다

| 변경 범위 | 규칙 |
|---|---|
| **내 소유 디렉터리 안** (3절) | PR 올리고 **셀프 머지 OK.** 팀 채팅에 한 줄만 |
| **공용 파일** (4절 표) | **다른 1명 승인 필수** |
| **확정 상수** (1절) | **3명 다 동의** |

내 디렉터리를 내가 망가뜨리면 나만 손해지만, 스키마나 상수를 잘못 머지하면 나머지 둘이 멈춘다. **승인은 남이 멈추는 변경에만 건다.**

### 늦은 머지가 나쁜 머지보다 위험하다

브랜치를 이틀 들고 있으면 아무리 꼼꼼히 리뷰해도 통합에서 터진다. 작게 자주 머지하면 충돌이 나도 10분짜리다.

- **PR 하나 = 작업 단위 하나.** `A2 등급 판정` 하나 끝나면 바로 PR. 400줄 PR은 사람이든 AI든 리뷰가 안 된다
- **하루 최소 한 번은 `main`에 머지한다.** 끝나지 않았으면 끝난 부분까지만
- 커밋은 작게, 자주. 하루치를 한 번에 올리면 충돌 해결이 불가능해진다

### 충돌이 나면 — 남의 커밋을 지우지 않는다

브랜치 보호는 **`main` 만** 지킨다. 내 브랜치 안에서 남의 변경을 날린 채 PR을 올리면 그대로 머지된다. **충돌 해결이 이 방식의 유일한 데이터 손실 경로다.**

- **한쪽을 통째로 고르지 않는다.** `--ours` · `--theirs` 로 뭉개면 남의 변경이 조용히 사라진다. 양쪽을 다 살리는 게 기본이고, 못 살리면 그 파일 담당자에게 묻는다
- **남의 디렉터리에서 난 충돌은 내가 해결하지 않는다.** 3절 소유권대로 담당자에게 넘긴다
- 브랜치가 꼬였으면 **고치려 하지 말고 버리고 다시 판다.** 작업 단위가 1~3시간짜리라 다시 파는 게 항상 싸다. `rebase` · `reset --hard` 로 되살리려다 더 잃는다
- **누가 "머지했다"고 알리면 그때 당긴다.** 특히 BE-2의 스키마. 내 브랜치가 그걸 필요로 하면 이어 붙이지 말고 `origin/main` 기준으로 새로 판다

머지 전 마지막 확인:

```bash
git diff origin/main --stat
```

**내가 만든 적 없는 파일이 보이면 머지하지 않는다.** 왜 거기 있는지 설명할 수 있어야 한다.

### 리뷰는 AI에게 시킨다

승인이 필요한 PR도 사람이 한 줄씩 읽지 않는다.

```bash
gh pr diff <번호> | claude -p "CLAUDE.md 규칙 위반을 검토해줘. 특히 확정 상수, 디렉터리 소유권, 요청하지 않은 파일 변경"
```

**PR을 올리는 쪽도** 올리기 전에 자기 클로드에게 `git diff origin/main` 을 읽히고 같은 것을 물어본다. 대부분 여기서 걸린다.

AI 리뷰가 잡는 것은 기계적인 것까지다. "이 설계가 애초에 맞나"는 PR이 아니라 계약을 정할 때 판단한다.

⚠️ **Windows 환경 주의**: 한글 커밋 메시지를 `git commit -m "..."` 으로 인라인 전달하면 PowerShell에서 인코딩이 깨진다. 파일 경유로 넘긴다.

```bash
git commit -F .commit-msg-tmp
```

`.commit-msg-tmp` 는 `.gitignore` 에 이미 들어 있다.

---

## 6. 코드

- **`fit/` 의 계산 로직은 순수 함수로 유지한다.** DB·네트워크·시간에 의존하지 않는다. 테스트가 쉬워지고, 이미지 생성이 실패해도 이 부분은 살아 있어야 한다
- **핏 등급 판정에는 테스트를 먼저 쓴다.** 경계값(정확히 6 / 14 / 24)과 신축성 보정 후의 경계까지. 실패를 눈으로 확인한 뒤 구현한다. 이 프로젝트에서 유일하게 TDD가 강제되는 지점이다
- 사용자에게 보이는 문구는 **한국어**, 이미지 생성 모델에 넘기는 프롬프트는 **영어**. 프롬프트는 사용자에게 절대 노출하지 않는다
- **API 응답 JSON 키는 `camelCase`, 경로는 소문자 복수형.** 프론트가 그대로 받아 쓴다. 파이썬 안에서는 `snake_case` 로 쓰고, **`core.schema.Schema` 를 상속하면 변환은 자동이다.** 손으로 맞추지 않는다
  ```python
  class FitReport(Schema):
      chest_ease: int
      fit_grade: str
  # GET /fittings/{id}  →  { "chestEase": 18, "fitGrade": "세미오버핏" }
  ```
- 그 밖의 코드 스타일(들여쓰기·따옴표·줄길이)은 **규칙으로 정하지 않는다.** 포매터도 안 건다 — 각자 다른 디렉터리를 쓰므로 스타일이 충돌할 일이 없고, 3일짜리에 쓸 시간이 아니다
- 프로필 치수는 값과 함께 **출처(실측 / 추정)** 를 저장하고 응답에 실어 보낸다. 프론트가 이걸로 표시를 구분한다
- 비밀번호는 **솔트 포함 단방향 해시**. 평문 저장 금지, 로그·에러 메시지·분석 도구에 남기지 않는다
- 전신 사진은 **서명 URL로만** 접근한다. 공개 URL을 만들지 않는다

---

## 7. 하지 말 것

| 하지 말 것 | 이유 |
|---|---|
| 요청하지 않은 리팩터링 | 남의 diff와 충돌한다. 눈에 거슬려도 놔둔다 |
| 남의 파일 포맷팅·정렬 | 실제 변경이 diff에 묻힌다 |
| 확정 상수를 "더 나은 값"으로 조정 | 1절 참조. 바꿔야 하면 팀에 말한다 |
| 새 의존성을 조용히 추가 | 4절 참조 |
| 스펙에 없는 기능 추가 | 3일 남았다. [plan.md](plan.md) 8절에 자른 것 목록이 있고, 그건 의도적으로 자른 것이다 |
| 자른 기능을 "간단하니까" 되살리기 | 위와 같음 |
| `main` 직접 push | 5절 참조 |
| 실패하는 테스트를 통과하게 고치기 | 테스트가 아니라 코드를 고친다 |

---

## 8. 막혔을 때

- **확정 상수를 모르겠다** → 1절. 없으면 PRD. 그래도 없으면 물어본다. 지어내지 않는다
- **남의 API 응답 모양을 모르겠다** → `docs/contracts.md`. 없으면 담당자에게 묻는다. 가정하고 만들지 않는다
- **내 영역 밖을 고쳐야 한다** → 담당자에게 말한다
- **시간이 부족하다** → 기능을 줄인다. [plan.md](plan.md) 8절에 자르는 기준이 있다
