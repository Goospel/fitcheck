# plan.md — FitCheck 백엔드 실행 계획

백엔드 3명이 **병렬로** 움직이기 위한 문서. 각자 자기 섹션만 읽어도 작업이 시작되게 썼다.

| | |
|---|---|
| 제출 마감 | **8/21(목) 오전 10시** |
| 코드 작업 가능 시간 | 8/20(수) 밤까지 — 약 3일 |
| 백엔드 인원 | 3명 (프론트·디자인은 별도 담당) |
| 상위 문서 | [PRD](https://www.notion.so/PRD-AI-FitCheck-3b722a79abb58106a343cf9c92b82e61) · [화면 설계서](https://www.notion.so/AI-FitCheck-3b722a79abb581498813d975f9ddc0ca) |

**범례** ✅완료 / 🔜다음 / ⬜예정 / ⏸의도적 보류 / ⚠️리스크·전제

---

## 1. 담당 배정

| 슬롯 | GitHub | 영역 | 총량 |
|---|---|---|---|
| **BE-1** | `Goospel` | 핏 엔진 & 계산 API | 약 11h |
| **BE-2** | `while-true-study` | 스키마 · 인증 · 프로필 · 공통 인프라 | 약 11h |
| **BE-3** | `jbkim4040` | 이미지 파이프라인 · 히스토리 · 결과 | 약 12h |

서로 바꿔도 무방하다. 다만 **BE-2의 스키마(B1)가 나머지 둘을 막으므로** 그 자리는 제일 먼저 착수 가능한 사람이 맡는다.

### 디렉터리 소유권 — 3개의 클로드가 같은 파일을 만지면 안 된다

| 슬롯 | 소유 디렉터리 |
|---|---|
| BE-1 | `fit/` · `api/garments` · `api/fittings` |
| BE-2 | `db/` · `auth/` · `api/profile` · `core/` |
| BE-3 | `images/` · `jobs/` · `api/history` · `api/results` |

**남의 디렉터리를 고쳐야 하면 직접 고치지 말고 담당자에게 말한다.** 바이브 코딩은 요청하지 않은 파일까지 건드리기 쉽다. 커밋 전에 `git diff --stat`으로 내 디렉터리 밖이 바뀌지 않았는지 확인한다.

---

## 2. 확정 상수 — 클로드가 지어내면 안 되는 값

⚠️ **이 절이 이 문서에서 제일 중요하다.** 세 명이 각자 클로드를 돌리면 셋 다 그럴듯한 숫자를 만들어낸다. 아래 값은 PRD에서 **확정**된 것이고 구현 중 임의 조정 금지다. 값이 어긋나면 리포트·이미지 프롬프트·비교 화면이 한꺼번에 깨진다.

### 핏 등급 임계값 (가슴 여유량 기준, cm)

```
        0        6        14       24
  ------|--------|--------|--------|------>
  타이트  슬림    레귤러   세미오버   오버
```

**하한 포함, 상한 미포함.** 여유 6cm면 레귤러핏, 14cm면 세미오버핏.

### 신축성 보정

| 입력 | 처리 |
|---|---|
| 좋음 | 각 구간 하한을 **6cm 낮춤** |
| 약간 | 각 구간 하한을 **4cm 낮춤** |
| 없음 | 보정 없음 |
| 미입력 | **"없음"으로 간주** (보수적 판정 — 큰 옷을 사게 하는 편이 낫다) |

### 여유량 계산식

| 부위 | 식 |
|---|---|
| 가슴 여유 | (의류 가슴단면 × 2) − 사용자 가슴둘레 |
| 허리 여유 | (의류 허리단면 × 2) − 사용자 허리둘레 · **입력된 경우만** |
| 어깨 차이 | 의류 어깨너비 − 사용자 어깨너비 |
| 소매 길이 차 | 의류 소매길이 − 사용자 팔길이 |

### 기장 5단계 — 이 문구 그대로, 다른 표현 금지

```
허리선 위      "허리 위로 올라오는 크롭 기장"
허리선        "허리에 딱 떨어지는 기장"
골반          "골반에 걸치는 기본 기장"
엉덩이 중간    "엉덩이를 반쯤 덮는 기장"
엉덩이 아래    "엉덩이를 완전히 덮는 롱 기장"
```

### 소매 3단계

```
손목 위 / 손목 / 손등 일부 덮음
```

"골반 아래" 같은 목록에 없는 표현을 만들지 않는다. 분기 개수가 여기서 확정된다.

---

## 3. 계약 3종 — Day 1 오전에 확정하고 공유

병렬 작업의 유일한 실패 모드는 **서로 다른 모양을 가정하고 각자 완성하는 것**이다. 아래 셋은 코드보다 먼저 타입·스키마로 못 박아 `main`에 올린다.

- [ ] ⬜ **계약 1 · DB 스키마** (BE-2) — `user` / `profile` / `garment` / `fitting` / `image_job`
- [ ] ⬜ **계약 2 · 핏 리포트 JSON** (BE-1) — BE-3의 프롬프트 변환과 프론트가 여기 의존
- [ ] ⬜ **계약 3 · 잡 상태 모델** (BE-3) — 대기 / 생성중 / 완료 / 실패 / 리포트만

확정되면 `docs/contracts.md` 한 파일에 모아 커밋한다. **작업 시작 전 각자 클로드에게 이 파일을 먼저 읽힌다.**

---

## 4. BE-1 · 핏 엔진 & 계산 API

DB도 API도 프론트도 필요 없이 시작할 수 있는 유일한 덩어리다. **가장 먼저 결과가 나와야 나머지 둘이 붙일 게 생긴다.**

- [ ] ⬜ **A1 · 여유량 계산** (30분) — 2절 계산식 4종. 허리는 입력 있을 때만 계산·표시
- [ ] ⬜ **A2 · 핏 등급 판정** (1h) — 임계값 0/6/14/24 + 신축성 보정
  - ⚠️ **테스트 먼저 쓴다.** 경계값(정확히 6, 정확히 14, 정확히 24)과 신축성 보정 후 경계까지. 실패를 눈으로 본 뒤 구현
- [ ] ⬜ **A3 · 기장·소매 랜드마크** (2h) — 키 → 상체 기준점 추정 → 기장 5단계 / 소매 3단계 문구 반환
- [ ] ⬜ **A4 · 신체 치수 추정기** (2.5h) — 키·몸무게·성별 → 어깨너비·가슴둘레·허리둘레·팔길이
  - ⚠️ PRD는 "사이즈코리아 통계"라 썼지만 3일 안에 그 데이터를 구해 정제할 수 없다. **성별별 근사 회귀식 하나**를 쓰고 계수는 상수로 빼둔다. 실측 입력이 있으면 이 값은 안 쓰인다
- [ ] ⬜ **A5 · 리포트 조립** (2h) — A1~A4를 묶어 JSON 반환 (= 계약 2)
  - 여유량 게이지 값 (5단계)
  - 신뢰도 배지: 어깨·가슴·허리를 직접 입력했으면 `실측`, 아니면 `추정`
  - 선호 핏 비교 문구 — **선호 핏 미설정이면 이 줄과 사이즈 추천 블록을 통째로 생략**하고 설정 유도 CTA 플래그를 내린다
- [ ] ⬜ **B3 · 의류 등록 API** (1.5h)
  - 필수: 사진 · 종류(티셔츠/셔츠/니트/후디/맨투맨) · 사이즈명 · 어깨너비 · 가슴단면 · 총장
  - 선택: 소매길이 · 허리단면 · 신축성
- [ ] ⬜ **B4 · 핏 분석 API** (1h) — 프로필 + 의류 → A5 호출 → 리포트 반환. **목표 100ms**
- [ ] ⬜ **F-10 · 사이즈 비교 API** (1h) — 같은 의류의 두 사이즈를 한 응답에 담아 반환
  - ⚠️ P1이지만 **자르지 말 것.** B4가 끝나면 계산을 두 번 돌리는 게 전부인데, PRD·화면 설계서 둘 다 "차별점이 가장 잘 드러나는 화면"이라 짚었다. 심사 시연에서 M과 L 게이지를 나란히 놓는 30초가 이 프로젝트에서 제일 강한 장면이다

---

## 5. BE-2 · 스키마 · 인증 · 프로필 · 공통 인프라

- [ ] 🔜 **B1 · DB 스키마 + 마이그레이션** (2h) — **최우선. 이게 나와야 BE-1의 B3, BE-3의 D4가 풀린다**
  - ⚠️ 착수 후 **2시간 안에** 초안을 `main`에 올린다. 완벽하지 않아도 된다. 없는 게 훨씬 나쁘다
  - `profile`은 치수 필드마다 **출처 플래그(실측/추정)를 같이 저장**한다. 화면 설계서 원칙 4가 이걸 요구하고, 나중에 넣으면 스키마를 다시 건드리게 된다
- [ ] ⬜ **공통 인프라** (2h) — 에러 응답 규격 · 로깅 · CORS · 환경변수 로딩 · 로컬 실행 방법
  - 에러 규격을 먼저 정해 공유한다. 안 그러면 세 명의 API가 각자 다른 모양으로 에러를 뱉는다
- [ ] ⬜ **C1 · 가입 · 로그인** (3h)
  - 가입: 이메일 → 비밀번호 설정 → 완료
  - 로그인: 이메일 + 비밀번호
  - ⚠️ **`POST /auth/check` 를 제일 먼저 만들어 프론트에 넘긴다.** PRD는 가입/로그인 탭을 두지 않고 "이메일 입력 후 서버가 판단해 분기"하는 설계다. 프론트의 로그인 화면 4단계(A→B/C→D)가 전부 이 응답 하나에 걸려 있다
  - 비밀번호는 **솔트 포함 단방향 해시**. 평문 저장·로그 노출 절대 금지
  - 만 14세 미만 가입 차단 (동의 체크)
- [ ] ⬜ **B2 · 프로필 API** (2h)
  - 1단계 필수 3개: 키(100~220 정수) · 몸무게(30~200 정수) · 성별(남/여/밝히지 않음)
  - 저장 즉시 **A4 추정기를 호출해** 나머지 치수를 채운다 → BE-1과의 유일한 연결점
  - 2단계 선택: 실측 4개(어깨·가슴·허리·팔) + 선호 핏
  - 프로필 정확도 `n/5` 계산 (실측 4개 + 선호 핏 1개)
  - "밝히지 않음" 선택 시 전체 평균 통계 적용, **모든 기능은 그대로 이용 가능**
- [ ] ⬜ **C5 · 로그아웃 · 계정 삭제** (1h) — 삭제 시 프로필·사진·피팅 결과·이메일 전부 즉시 삭제

---

## 6. BE-3 · 이미지 파이프라인 · 히스토리 · 결과

- [ ] ⬜ **D3 · 프롬프트 변환** (1.5h) — **스키마를 안 기다려도 되는 순수 함수. 여기부터 시작한다**
  - 계산 결과 → 영어 지시문. PRD 6.3절에 매핑표가 완성돼 있으니 그대로 옮긴다

  | 계산 결과 | 지시 |
  |---|---|
  | 가슴 여유 +18cm → 세미오버핏 | 약간 넉넉한 핏으로, 가슴에 여유가 있게 |
  | 어깨 +4cm → 드롭숄더 | 어깨 봉제선을 실제 어깨보다 아래로 |
  | 기장 → 엉덩이 중간 | 밑단이 엉덩이를 반쯤 덮게 |
  | 소매 → 손등 일부 덮음 | 소매가 손등을 일부 덮게 |

  - **실제 전달은 영어로.** 이 문장은 사용자에게 절대 노출되지 않는다
- [ ] ⬜ **D1 · 사진 업로드 · 저장** (2h) — 암호화 저장, **서명 URL로만 접근** (유효기간 제한)
- [ ] ⬜ **D2 · 품질 검증 — 해상도만** (20분) — 512×768 미만 거부
  - ⏸ 인물 감지 · 전신 여부 · 정면 판별 · 부적절 이미지 필터는 **보류**. 각각 별도 비전 모델이 필요하고 6시간+짜리다
- [ ] ⬜ **D4 · 비동기 잡 큐 + 상태 폴링** (4h) — 창을 닫거나 재접속해도 결과가 유실되지 않을 것. 동시 처리 최소 5건
- [ ] ⬜ **D5 · 타임아웃 · 실패 처리** (1h) — 10분 초과 시 실패 처리하되 **핏 리포트는 유지**. 사용자가 빈손으로 돌아가지 않게
- [ ] ⬜ **D6 · 중복 생성 방지** (1h) — 동일 (사용자·의류·사이즈) 재요청 시 기존 결과 재사용
  - ⚠️ **자르지 말 것.** 생성 1건이 최대 10분 + 유료 API 호출이다. 시연 중 같은 조합을 두 번 누르면 그대로 비용이고, 심사위원 앞에서 10분을 기다리는 사고가 난다. 1시간짜리 작업이 사고 하나를 막는다
- [ ] ⬜ **B5 · 히스토리 API** (1.5h) — 상태별 목록 + 완료 개수 뱃지
  - ⚠️ 편의 기능이 아니다. 생성이 10분 걸리는 이상 **결과를 나중에 찾을 유일한 경로**라서 P0다
- [ ] ⬜ **B6 · 결과 조회 API** (1h) — 이미지 + 리포트 통합. 이미지 없는 경로(F-09)에서도 성립할 것

---

## 7. 타임라인

| | BE-1 | BE-2 | BE-3 |
|---|---|---|---|
| **Day 1 오전** | A1 · A2 (테스트 먼저) | **B1 스키마 → 즉시 공유** | D3 프롬프트 변환 |
| **Day 1 오후** | A3 · A4 | 공통 인프라 · C1 | D1 · D2 |
| **Day 2 오전** | A5 → **계약 2 공유** | C1 마무리 · `/auth/check` 넘기기 | D4 |
| **Day 2 오후** | B3 · B4 | B2 (A4 연결) | D4 · D5 · D6 |
| **Day 3 오전** | F-10 비교 | C5 · 프론트 연동 지원 | B5 · B6 |
| **Day 3 오후** | **셋이 모여 통합 · 시연 리허설** | | |

Day 3 오후는 새 기능을 만들지 않는다. 통합에서 반드시 문제가 나오고, 그때 고칠 시간이 없으면 시연이 깨진다.

---

## 8. ⏸ 의도적으로 자른 것

발표에서 숨기지 말고 **"설계는 끝났고 구현은 이후"**로 말한다. PRD 8.2절이 그대로 근거가 되므로, 오히려 설계 깊이를 보여주는 재료가 된다.

| 항목 | 이유 |
|---|---|
| C2 이메일 6자리 인증 코드 | 메일 발송 인프라(SES·SendGrid) 붙이는 데 반나절. 심사위원 눈에는 "로그인이 된다" 이상으로 안 보인다 |
| C3 새 기기 감지 추가 인증 | 시연에서 발생하지 않는 경로 |
| C4 비밀번호 재설정 | 위와 같음 |
| D2 비전 검증 (해상도 제외) | 별도 모델 필요, 6h+ |
| E1 보관기간 자동삭제 (30일·90일) | 배치 잡이라 3일 시연 중 한 번도 안 돈다 → **정책 문서로 제출** |
| E2 이상 로그인 알림 | 위와 같음 |
| E3 1년 미접속 계정 삭제 | 위와 같음 |

---

## 9. ⚠️ 리스크

| # | 리스크 | 대응 |
|---|---|---|
| R1 | **이미지 생성 모델 미정** (PRD Q1) — "후보 2~3개 실측 비교"할 시간이 없다 | Day 1 오전에 하나 찍는다. D3만 교체 가능하게 두면 나중에 바꿔도 다른 코드가 안 흔들린다 |
| R2 | **10분 비동기 작업이 스택 선택을 제약** | 서버리스 계열은 함수 타임아웃에 걸린다. D4를 어디서 돌릴지가 언어보다 먼저 정해져야 한다 |
| R3 | **B1 스키마 지연이 전체를 막는다** | 완벽 대신 속도. 2시간 내 초안을 올리고 이후 마이그레이션으로 고친다 |
| R4 | **클로드 3개가 같은 파일을 건드린다** | 1절 디렉터리 소유권. 커밋 전 `git diff --stat`으로 내 영역 밖이 안 바뀌었는지 확인 |
| R5 | **클로드가 확정 상수를 지어낸다** | 2절을 각자 세션 시작 시 먼저 읽힌다. 특히 임계값과 기장 문구 |
| R6 | 총량 34h를 3일에 넣는 계획이라 여유가 없다 | 막히면 8절로 더 내린다. **A그룹과 F-10은 마지막까지 지킨다** — 이게 없으면 서비스가 아니라 그냥 이미지 생성기다 |

---

## 10. 작업 규칙

- `main`에서 직접 작업하지 않는다. `feat/...` 브랜치 → PR → 머지
- 브랜치를 딸 때 `git fetch origin` 후 `origin/main` 기준으로 딴다
- **A2 등급 판정은 테스트를 먼저 쓰고 실패를 확인한 뒤 구현한다.** 나머지는 상황에 맞춰서
- 작업을 끝내면 이 문서의 체크박스를 ✅로 바꾼다
