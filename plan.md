# plan.md — FitCheck 백엔드 실행 계획

백엔드 3명이 **병렬로** 움직이기 위한 문서. 각자 자기 섹션만 읽어도 작업이 시작되게 썼다.

> **누가 뭘 언제** 는 이 문서, **어떻게 쓰는가** 는 [CLAUDE.md](CLAUDE.md). 확정 상수·기술 스택 규약·디렉터리 소유권·git 규칙은 CLAUDE.md에 있고 클로드가 매 세션 자동으로 읽는다.

| | |
|---|---|
| 제출 마감 | **8/21(목) 오전 10시** |
| 코드 작업 가능 시간 | 8/20(수) 밤까지 — 약 3일 |
| 백엔드 인원 | 3명 (프론트·디자인은 별도 담당) |
| 상위 문서 | [PRD](https://www.notion.so/PRD-AI-FitCheck-3b722a79abb58106a343cf9c92b82e61) · [화면 설계서](https://www.notion.so/AI-FitCheck-3b722a79abb581498813d975f9ddc0ca) |

**범례** ✅완료 / 🔜다음 / ⬜예정 / ⏸의도적 보류 / ⚠️리스크·전제

---

## 1. 담당 배정

| 슬롯 | 이름 | GitHub | 영역 | 총량 |
|---|---|---|---|---|
| **BE-1** | **KimZion** | `Goospel` | 핏 엔진 & 계산 API | 약 11h |
| **BE-2** | **KimZion** ⚠️ 인수 | `Goospel` | 스키마 · 인증 · 프로필 · 인프라 · 배포 | 약 8.5h |
| **BE-3** | **김정빈** | `jbkim4040` | 이미지 파이프라인 · 히스토리 · 결과 | 약 8.5h |

BE-1이 제일 무겁고 BE-2·BE-3은 앞쪽에 몰려 있다. **BE-2가 먼저 비므로 남는 시간은 통합·프론트 연동 지원에 쓴다.**

⚠️ **BE-2를 KimZion이 인수했다 (8/19).** 맹동훈은 8/18에 레포에 들어왔지만 하루 동안 커밋·브랜치가 없었고, B1 스키마가 없으면 B3·D4가 전부 막힌 채 마감을 맞는다. 원래 담당을 지우는 것이 아니라 **막힌 것을 먼저 뚫은 것**이다.

> **맹동훈이 작업을 시작한다면** — `db/models.py` 는 이미 `main` 에 있다. 다시 만들지 말고 팀에 말한 뒤 이어받는다. 남은 BE-2 작업은 C1(인증) · B2(프로필) · C5 이고, 그중 아직 아무도 손대지 않은 것을 먼저 가져가면 충돌이 없다.

✅ **Supabase 프로젝트 생성 완료** (2026-08-19 · `fitcheck` · Seoul). 실 DB 에 테이블 5개가 올라갔고 전 구간이 돈다.

✅ **Railway 배포 완료** (2026-08-19 · `https://web-production-19ef6.up.railway.app`). `main` 에 머지하면 자동으로 다시 뜬다.

⚠️ **사람이 해야 할 것은 이제 「값을 정하는 것」뿐이다** — 계정 가입은 다 끝났다. 남은 것은 [Q1·Q2·Q3](docs/open-questions.md)(기장·소매 판정 기준, 치수 추정 계수)로, 지어내면 리포트가 조용히 틀리는 자리라 비워 뒀다.

디렉터리 소유권은 [CLAUDE.md 3절](CLAUDE.md)에 있다. 확정 상수(임계값·기장 문구 등)는 [CLAUDE.md 1절](CLAUDE.md).

---

## 2. 기술 스택 — 확정

| | 선택 | 왜 |
|---|---|---|
| 언어·프레임워크 | **Python 3.12 + FastAPI** | 이미지 모델 SDK가 전부 파이썬 우선 · `/docs` 자동 생성이 프론트와의 계약을 대신한다 |
| 패키지 | **uv** | 3명 환경이 lock 하나로 같아진다 |
| DB | **Supabase Postgres — 팀 공용 1개** | 로컬 설치 0분. 스키마가 적용된 순간 셋 다 즉시 본다 |
| ORM | **SQLAlchemy 2.0 async + asyncpg** | CRUD 물량이 많다. **마이그레이션 도구는 안 쓴다** |
| 파일 저장 | **Supabase Storage** | 서명 URL이 내장. 직접 만들면 2시간 |
| 인증 | **직접 JWT** (bcrypt + PyJWT) | 프론트가 별도 레포라 인증 창구는 하나여야 한다 |
| 잡 큐 | **Postgres 테이블 + 인프로세스 워커** | Redis·Celery 안 붙인다 |
| 이미지 생성 | **OpenAI `gpt-image-2`** (D0 확정) | 팀 크레딧으로 카드 없이 쓴다. 모델 교체는 환경변수 하나 |
| 배포 | **Railway** | 10분짜리 잡이 있어 서버리스는 탈락 (R2) |
| 테스트 | **pytest** | A2 등급 판정용. 그 외엔 강제하지 않는다 |

코드에서 지켜야 할 규약(camelCase 베이스·에러 규격·큐 구현)은 [CLAUDE.md 2절](CLAUDE.md).

### 계정 준비 — Day 1 오전에 끝낼 것

| 무엇 | 누가 | 비고 |
|---|---|---|
| ~~Supabase 프로젝트 1개~~ | ~~맹동훈~~ → KimZion | ✅ 완료. `DATABASE_URL` 은 **Session pooler** 문자열을 쓴다 — 형식은 `.env.example` 주석 |
| ~~Replicate 토큰~~ | ~~김정빈~~ | ✅ **필요 없어졌다.** OpenAI 크레딧으로 확정([Q5](docs/open-questions.md)) — 예비 경로를 코드에서 걷어냈다 |
| ~~Railway 프로젝트~~ | ~~맹동훈~~ → KimZion | ✅ 완료. 배포 주소는 이 문서 위쪽 |

### 로컬 실행

```bash
uv sync
cp .env.example .env
uv run uvicorn main:app --reload
```

`http://localhost:8000/docs` 에 지금까지 만들어진 API가 전부 뜬다. **프론트에 이 주소를 주면 계약 문서가 완성되기를 기다릴 필요가 없다.**

---

## 3. 계약 3종 — Day 1 오전에 확정하고 공유

병렬 작업의 유일한 실패 모드는 **서로 다른 모양을 가정하고 각자 완성하는 것**이다. 아래 셋은 코드보다 먼저 타입·스키마로 못 박아 `main`에 올린다.

- [x] ✅ **계약 1 · DB 스키마** (KimZion 인수) — `app_user` / `profile` / `garment` / `fitting` / `image_job` 를 [docs/contracts.md](docs/contracts.md)에 확정. **B3 · D4 이제 시작 가능**
- [x] ✅ **계약 2 · 핏 리포트 JSON** (KimZion) — [docs/contracts.md](docs/contracts.md)에 확정. **D3와 프론트는 이제 시작 가능**
- [x] ✅ **계약 3 · 잡 상태 모델** (KimZion 인수) — 대기 / 생성중 / 완료 / 실패 / 리포트만. `db/models.py` 의 `FITTING_STATUSES` 가 유일한 출처고, `GET /fittings` 의 뱃지 개수가 이 다섯을 그대로 센다

확정되면 `docs/contracts.md` 한 파일에 모아 커밋한다. **작업 시작 전 각자 클로드에게 이 파일을 먼저 읽힌다.**

---

## 4. BE-1 · KimZion — 핏 엔진 & 계산 API

DB도 API도 프론트도 필요 없이 시작할 수 있는 유일한 덩어리다. **Supabase가 아직 없어도 지금 시작한다.**

- [x] ✅ **A1 · 여유량 계산** — `fit/ease.py`. 계산식 4종. 허리·소매는 선택 입력이라 없으면 `None` (여유량 0과 구분)
  - ⚠️ **결과를 소수 한 자리로 맞춘다 (8/20 추가).** 안 하면 이진 부동소수점 찌꺼기가 그대로 화면에 나간다 — 배포 서버에서 실제로 **`가슴 여유 8.799999999999997cm`** 이 응답에 실렸다(`54.0 × 2 − 99.2`). 계약 2 의 `chestEase` 는 프론트가 그대로 그리는 값이다
    - **A4 가 만든 문제는 아니지만 A4 가 드러냈다** — 실측만 쓰던 때는 `92.0`·`51.0` 처럼 0.0 으로 끝나 우연히 딱 떨어졌다. 추정값은 1자리 소수라 항상 걸린다. **「테스트가 다 통과하는데 배포에서 보였다」는 종류**라 로컬 스모크에 값을 눈으로 찍는 줄을 남겨 뒀다
    - **등급도 반올림한 값으로 판정한다** — 안 그러면 「여유 6.0cm · 슬림핏」처럼 화면이 자기모순을 일으킨다. 임계값 0/6/14/24 는 그대로고 비교에 넣는 값의 정밀도만 정한 것이다
    - 테스트 28건 · 돌연변이 6종 사살
- [x] ✅ **A2 · 핏 등급 판정** — `fit/grade.py`. 임계값 0/6/14/24 + 신축성 보정
  - 테스트 먼저 쓰고 스텁으로 실패를 확인한 뒤 구현. 돌연변이 2종(경계 포함·보정 부호)으로 판별력 검증
  - ⚠️ 등급 문구 5종은 `fit.grade.GRADE_ORDER` 에 있다. BE-3의 D3는 문자열을 새로 쓰지 말고 여기서 import 한다
  - ⚠️ **0cm 미만은 「너무 작음」이다** (PRD 6.2.2). 「타이트핏」으로 잘못 넣었던 것을 8/19 교정했다
- [x] ✅ **A3 · 기장 판정** — `fit/length.py`. 총장 + 사용자 키 → 기장 5단계 문구 ([Q1](docs/open-questions.md) 종결)
  - **경계를 지어내지 않았다.** 문구 5종이 실제 랜드마크를 그대로 부르고 있어서 사이즈코리아 8차에서 그 높이를 받아 왔다 — `허리높이` · `엉덩위높이/Top-hip높이` · `엉덩이높이` · `볼기고랑높이`, 기준점은 `목뒤높이`. 경계 = `목뒤높이 − 랜드마크높이` 를 키 비율로 스케일
  - **어젯밤 「남은 결정」이라 적어둔 게 결정이 아니었다** — HPS 는 `어깨높이`(어깨점)가 아니라 `목옆높이`(목옆점)다. 제대로 재니 목뒤와 **0.67cm** 차이라 밴드 폭(8~10cm) 안에서 판정을 못 바꾼다. 표본이 두 배인 `목뒤높이`(직접측정 n=789/714)로 갔다
  - ⚠️ **알려진 근사 하나** — 총장은 평면 실측인데 랜드마크 높이는 수직 높이라, 입으면 등 곡면을 타고 내려가 실제 도달점이 계산보다 조금 위다. 실착 데이터가 없어 `DRAPE_ALLOWANCE = 0.0` 으로 두고 **테스트로 못 박았다** — 값 하나만 올리면 경계 4개가 같이 움직인다
  - **성별은 기장에 거의 영향이 없다** — 키로 정규화하면 남녀 차가 최대 0.9cm. A4(둘레)와 딴판이라 「밝히지 않음」이 손해가 아니다
  - **D3 프롬프트가 공짜로 늘었다** — `images/prompt.py` 를 한 줄도 안 고쳤는데 `The hem covers half of the buttocks.` 가 붙어 나간다. 매핑을 미리 넣어 둔 게 값을 했다
  - 테스트 55개 · 돌연변이 9종 전부 사살. ⚠️ 돌연변이 스크립트가 처음엔 **거짓 통과**했다 — `DRAPE_ALLOWANCE = 0.0` 이 docstring 에도 있어서 `replace(...,1)` 이 주석만 바꿨다. 패턴 유일성 검사를 넣어 잡았다
- [x] ✅ **A3-소매 · 소매 3단계** — `fit/length.py` 의 `sleeve_label()`. 밴드 폭 **±2cm 대칭** ([Q2](docs/open-questions.md) 종결 · 2026-08-20 승인)
  - **근거가 반씩 다르고 그 구분을 코드에 남겼다** — 「차이 0 = 손목」은 사이즈코리아 `팔길이`(어깨점~손목)가 답하지만, **밴드 폭은 인체 데이터가 아니라 합의값**이다. 「데이터로 정했다」고 말하면 절반만 참이 된다
  - **비대칭을 만들지 않았다** — 입으면 소매가 올라가니 +쪽에 관대해야 한다는 주장이 가능하지만, 그건 `DRAPE_ALLOWANCE` 와 똑같이 실착 데이터가 있어야 넣는 보정이다
  - ⚠️ **반팔은 「손목 위」로 나온다.** 문구 3종이 확정이라 「반팔」을 새로 만들지 않았다 — 시연 티셔츠 3벌이 전부 `sleeveDiff ≈ −38cm` 라 셋 다 같은 문구다
  - ⚠️ **머지하면 D6 재사용이 한 번 끊긴다.** 저장된 스냅샷은 `sleeveLabel: null` 인데 새 판정은 값이 있어서 `_reusable()` 의 dict 전체 비교가 다르게 본다. **시연 전에는 머지하지 않는다**
  - 테스트 13건 신설 · 돌연변이 **8종 전부 사살** (밴드 폭 양방향·경계 포함 뒤집기·부호 반전·문구 순서·모르는 값 뭉개기). ⚠️ 돌연변이 스크립트가 처음엔 1종을 놓쳤다 — 레포가 CRLF 인데 패턴에 `\n` 을 써서 **0건으로 조용히 빗나갔다**
- [x] ✅ **A4 · 신체 치수 추정기** — `fit/estimate.py`. **키·몸무게·성별만으로도 리포트가 나온다** ([Q3](docs/open-questions.md) 종결)
  - **PRD가 지목한 그 출처를 실제로 받아 왔다** — 사이즈코리아 제8차 인체치수조사 20~24세 평균(sizekorea.kr 「인체 항목 검색」, 표본 항목당 남 789~798 · 여 714~716)

    | | 키 | 몸무게 | 가슴 | 허리 | 어깨 | 팔 |
    |---|---|---|---|---|---|---|
    | 남성 | 174.99 | 72.68 | 101.04 | 82.06 | 40.01 | 59.06 |
    | 여성 | 161.79 | 55.31 | 88.17 | 72.62 | 34.70 | 54.10 |

  - **스케일**: 길이(어깨·팔) ∝ 키 · 둘레(가슴·허리) ∝ √(몸무게/키). 뒤엣것은 「단면적 ≈ 부피/키, 둘레 ∝ √단면적」에서 온다 — 닮은꼴로 커지면 둘레도 키에 비례하는 성질을 만족한다
  - ⚠️ **앵커는 실측이지만 스케일 법칙은 가정이다.** 조사가 주는 것은 평균·분위수지 조건부 추정이 아니다. 이 구분을 지우지 말 것 — 특히 허리는 살이 먼저 붙어 실제로는 가슴보다 몸무게에 민감한데, 지수를 눈대중으로 손대는 것이 원시 데이터 없이 할 수 있는 최악이라 같은 식을 쓴다
  - ⚠️ **`MEASUREMENTS_REQUIRED` 가 없어졌다** — 프론트에 그 분기가 있으면 지운다 (계약 변경, docs/contracts.md 갱신함)
  - ⚠️ **정확도 `n/5` 는 그대로 실측만 센다.** 추정으로 채웠다고 점수가 오르면 배지가 거짓말이 된다
  - 실측이 있으면 실측이 이긴다. 어디까지가 추정인지는 `confidence` 가 들고 간다
  - 테스트 30건 · 돌연변이 **14종 전부 사살** (앵커 변조·스케일 뒤집기·실측/추정 우선순위 역전·배지 위조 포함)
- [x] ✅ **A5 · 리포트 조립** — `fit/report.py`. 계약 2 = [docs/contracts.md](docs/contracts.md)
  - 여유량 게이지 `gaugeLevel` 1~5 · 신뢰도 배지 `confidence` · 선호 핏 비교 `gradeDistance` + CTA 플래그
  - **문구는 백엔드가 만들지 않는다.** 판정만 내고 문장 조립은 프론트가 한다 — 미확정 문구(Q1·Q2·Q4)에 막히지 않으려는 선택이다
  - `lengthLabel` · `sleeveLabel` 은 A3가 풀릴 때까지 항상 `null`. 자리는 이미 계약에 있다
- [x] ✅ **B3 · 의류 등록 API** — `api/garments.py`. `POST`·`GET /garments`
  - 종류 5종·신축성 3종을 `Literal` 로 막는다. **신축성 문구는 `fit.grade.STRETCH_RELIEF` 에서 가져온다** — 「보통」 같은 값이 통과하면 보정이 조용히 0 이 된다
  - 한 행이 한 사이즈다. 같은 옷의 M·L 은 두 번 등록하고 F-10 이 둘을 나란히 놓는다
  - ⚠️ **`photoPath` 를 요청에서 뺐다** (D1). 경로를 클라이언트가 정하면 남의 사진을 가리키는 행을 만들 수 있다 — 사진은 `PUT /photos/garments/{id}` 가 붙인다
  - 테스트 35건. 돌연변이 3종(신축성 검증·소유자 필터·정렬) 사살
- [x] ✅ **B4 · 핏 분석 API** — `api/fittings.py`. `POST /fittings` · `GET /fittings/{id}`
  - 리포트를 **그 시점 스냅샷으로** 저장한다. 프로필이 나중에 바뀌어도 이미 만든 판정은 그대로다
  - ⚠️ **A4 가 없어 가슴·어깨 실측이 없으면 400 `MEASUREMENTS_REQUIRED` 로 거절한다.** 지어낸 값으로 리포트를 내지 않는다 ([Q3](docs/open-questions.md))
  - 남의 의류·남의 리포트는 「없다」로 답한다 — 존재 여부를 흘리지 않는다
- [x] ✅ **F-10 · 사이즈 비교** — 같은 의류의 여러 사이즈를 한 응답에 담아 반환
  - [x] ✅ 계산 — `fit/compare.py`. `compare_sizes(몸, {"M": 옷M, "L": 옷L})` → 사이즈별 리포트 + `recommendedSize`
    - 리포트는 계약 2를 **중첩해 그대로** 쓴다. 프론트가 단일 리포트 컴포넌트를 재사용한다
    - 추천 = 선호 핏과의 단계 차 최소. 동점이면 **더 헐렁한 쪽** (큰 옷이 낫다 — 1절 원칙)
    - ⚠️ **선호 핏이 없으면 추천하지 않는다** (`null`). 근거가 없어 지어내지 않았다 — 대신 `showPreferenceCta`
    - 돌연변이 3종(동점 방향 · `abs` 제거 · 미설정 시 첫 사이즈)으로 테스트 판별력 확인
  - [x] ✅ 엔드포인트 — `POST /fittings/compare`. **저장하지 않는다** — 계산을 여러 번 돌릴 뿐이다
    - 사이즈명이 겹치면 dict 로 모으다 조용히 합쳐진다 → 400 `DUPLICATE_SIZE_NAME` 로 알린다
  - ⚠️ P1이지만 **자르지 말 것.** B4가 끝나면 계산을 두 번 돌리는 게 전부인데, PRD·화면 설계서 둘 다 "차별점이 가장 잘 드러나는 화면"이라 짚었다. 심사 시연에서 M과 L 게이지를 나란히 놓는 30초가 이 프로젝트에서 제일 강한 장면이다

---

## 5. BE-2 · KimZion 인수 (원래 맹동훈) — 스키마 · 인증 · 프로필 · 인프라 · 배포

- [x] ✅ **B0 · Supabase 프로젝트 + Railway 연결** — **이게 없으면 아무도 DB를 못 쓴다**
  - [x] ✅ Supabase 프로젝트 생성 — `fitcheck` · Seoul(ap-northeast-2) · PostgreSQL 17.6. 테이블 5개 · 외래키 CASCADE 5개 확인
  - [x] ✅ `db/session.py` (async 엔진 + 세션) — 엔진은 **처음 쓸 때** 만든다. `DATABASE_URL` 이 없어도 앱은 뜬다
  - [x] ✅ 실 DB 스모크 22건 통과 — 가입 → 프로필 → 의류 → 핏 리포트 → 사이즈 비교 → 히스토리 → 탈퇴(CASCADE). **인메모리 SQLite 로만 돌던 코드가 진짜 Postgres 에서 그대로 돌았다**
  - ⚠️ **연결은 Session pooler 로만 된다.** Direct connection(`db.<ref>.supabase.co`)은 **IPv6 전용**이고(실측 — A 레코드가 없다), Transaction pooler(6543)는 asyncpg 의 prepared statement 를 못 받는다
  - ⚠️ **Data API(PostgREST)는 꺼 뒀다.** 켠 채로 두면 anon 키만으로 전 테이블이 공개 REST 에 열린다 (PRD 8.2). 우리는 SQLAlchemy 로 직접 붙어서 필요가 없다. **Storage 는 영향 없다** — D1 은 그대로 쓴다
  - [x] ✅ Railway 배포 — `https://web-production-19ef6.up.railway.app` · `main` push 시 자동 재배포. 배포 주소로 스모크 22건 재통과
  - ⚠️ **리전을 Singapore 로 옮겼다.** 기본값 EU West 로는 DB(서울) 왕복에 **쿼리당 1.3초**가 붙었다 (실측 `/auth/check` 1.70s → 0.60s). 앱과 DB 를 같은 대륙에 두지 않으면 시연에서 티가 난다
  - ⚠️ Railway 는 변수 변경을 **스테이징만** 하고 별도 `Deploy` 를 눌러야 반영된다 — 변수를 넣었는데 503 이면 이걸 의심한다
  - ⚠️ **머지했다고 배포된 게 아니다 (8/20 실측).** #33 을 머지했는데 **30분이 지나도 빌드가 시작조차 안 됐다.** 설정은 멀쩡했다 — 레포·브랜치(`main`)·「Auto deploys when pushed to GitHub」 켜짐·「Wait for CI」 꺼짐, GitHub Actions 도 체크런도 0건. **푸시 이벤트 자체가 유실된 것**이다. 스킵 목록(`Show Skipped`)에도 없었다
    - **확인법**: `/health` 로는 못 잡는다 — 새 컨테이너가 못 뜨면 Railway 가 **옛 것을 계속 띄워 두므로** 어느 쪽이든 200 이다. Railway → 서비스 → Deployments 에서 **ACTIVE 항목의 커밋 메시지**를 봐야 한다
    - ⚠️ **`Redeploy` 는 최신 커밋을 안 당긴다** — 그 배포의 **같은 커밋**을 다시 빌드한다(실측: #33 머지 후 눌렀는데 새 빌드가 #32 메시지로 떴다). 최신을 올리려면 **새 푸시**가 필요하다
    - 되살리는 법: `main` 에 새 커밋을 하나 올린다(= 다음 PR 을 머지한다). 머지된 것은 `main` 에 이미 있으므로 **다음 배포에 같이 실려 나간다** — 유실된 변경을 따로 챙길 필요는 없다
  - ⚠️ **에러 규격·CORS·환경변수 로딩·camelCase 베이스는 `core/` 에 이미 있다.** 다시 만들지 말고 인계받아 쓴다
- [x] ✅ **B1 · DB 스키마** — `db/models.py`. 5테이블 = 계약 1. **B3 · D4 가 여기서 풀렸다**
  - 테이블명은 `user` 가 아니라 **`app_user`** — `user` 는 예약어라 `select * from user` 가 조용히 틀린다
  - 치수는 **NULL = 추정**. 출처 플래그 컬럼 4개를 두지 않고 파생시킨다 (A4 가 나중에 붙어도 기존 행이 자동으로 채워진다)
  - ORM `relationship` 을 두지 않는다 — async 에서 lazy load 는 `MissingGreenlet` 으로 터진다
  - DB 없이 검증한다: PostgreSQL 방언으로 DDL 컴파일 + 제약 확인. 테스트 38건
  - ⚠️ 이후 스키마 변경은 `create_all()` 이 안 잡는다. **BE-2가 손으로 `ALTER` 를 돌리고 팀에 알린다.** 3일간 몇 번 안 일어난다
  - `profile`은 치수 필드마다 **출처 플래그(실측/추정)를 같이 저장**한다. 화면 설계서 원칙 4가 이걸 요구하고, 나중에 넣으면 스키마를 다시 건드리게 된다
- [x] ✅ **C1 · 가입 · 로그인** — `auth/security.py` · `api/auth.py`. 응답 모양은 [docs/contracts.md](docs/contracts.md) 부록
  - `POST /auth/check` · `/auth/signup` · `/auth/login` · `GET /auth/me`. 가입하면 토큰을 바로 준다
  - ⚠️ **비밀번호 72바이트 상한은 bcrypt 한계다 — 한글 24자면 걸린다.** 안 막으면 500 ([T7](docs/open-questions.md))
  - 이메일은 소문자로 정규화한다. `A@B.com` 으로 중복 계정이 생기지 않게
  - 가입 중복은 미리 SELECT 하지 않고 **유니크 제약이 잡게** 둔다 — 확인과 삽입 사이의 경쟁이 사라진다
  - 테스트 36건. 인메모리 SQLite 로 진짜 DB 를 띄운다 (공용 DB 에 붙지 않는다). 돌연변이 4종 사살
  - ⚠️ **`POST /auth/check` 를 제일 먼저 만들어 프론트에 넘긴다.** PRD는 가입/로그인 탭을 두지 않고 "이메일 입력 후 서버가 판단해 분기"하는 설계다. 프론트의 로그인 화면 4단계(A→B/C→D)가 전부 이 응답 하나에 걸려 있다
  - 비밀번호는 **bcrypt 해시**. 평문 저장·로그 노출 절대 금지
  - 만 14세 미만 가입 차단 (동의 체크)
- [x] ✅ **B2 · 프로필 API** — `api/profile.py`. `GET`·`PUT /profile` 하나로 두 단계를 다 받는다
  - 키 100~220 · 몸무게 30~200 · 성별 3지선다를 **API 와 DB 양쪽에서** 막는다
  - 치수는 값과 출처를 같이 보낸다: `{"value": 44.0, "source": "실측"}`. 안 넣으면 `{"value": null, "source": "추정"}`
  - 정확도 `n/5` — 실측 4개 + 선호 핏 1개. 선호 핏 문구는 `fit.grade.GRADE_ORDER` 에서 가져온다
  - ⚠️ **저장 시점에 A4 를 호출하지 않는다.** 빈 칸이 곧 「추정」이므로 읽을 때 채운다 — A4 가 나중에 붙어도 기존 프로필이 자동으로 값을 얻는다 (db/models.py 설계 판단 ②)
  - 「밝히지 않음」도 기능 제약이 없다. 정확도 계산까지 동일
  - 테스트 36건. 돌연변이 3종(정확도 식·출처 판정·PUT 전체 교체) 사살
- [x] ✅ **C5 · 계정 삭제** — `DELETE /auth/me`. 프로필·의류·피팅이 외래키 CASCADE 로 같이 사라진다
  - ⚠️ **로그아웃 엔드포인트는 만들지 않았다.** JWT 는 서버에 상태가 없어 할 일이 없다 — 프론트가 토큰을 버리면 그게 로그아웃이다
  - ⚠️ **저장소의 사진을 먼저 지운다** (D1). 순서가 반대면 행을 잃은 채 파일만 남아 누구 것인지 알 수 없다
  - ⚠️ **SQLite 는 외래키를 기본으로 안 지킨다.** 테스트에서 `PRAGMA foreign_keys=ON` 을 켜지 않으면 CASCADE 확인이 통째로 거짓이 된다
- [ ] ⬜ **통합 지원** (남는 시간) — 프론트 연동 질문 대응 · 배포 재기동 · Day 3 통합

---

## 6. BE-3 · KimZion 인수 (원래 김정빈) — 이미지 파이프라인 · 히스토리 · 결과

⚠️ **2026-08-19부터 BE-1(KimZion)이 BE-3 도 겸한다** — 원 담당이 이틀째 착수하지 않았고 이미지 생성이 PRD 의 핵심 기능이다. 김정빈이 작업을 시작한다면 팀에 말한 뒤 아직 손대지 않은 항목부터 가져간다.

- [x] ✅ **D0 · 이미지 모델 스파이크** — **끝. OpenAI `gpt-image-2` 로 확정한다**
  - [x] ✅ **OpenAI 로 간다.** 멋사 팀 조직(`호서대 멋사 A팀`)에 **크레딧 $102.81** 이 있어 카드 없이 쓴다. `gpt-image-1/1.5/2` 전부 열려 있는 것을 확인했다 (조직 인증 게이트 안 걸림)
    - ⚠️ **팀 공용 크레딧이다.** 다른 팀원 몫이 같이 들어 있으니 무한정 돌리지 않는다. D6(중복 생성 방지)가 여기서 값을 한다
    - 프로젝트를 `fitcheck` 으로 따로 팠다 — 이번 해커톤 지출만 따로 집계된다
    - ⚠️ OpenAI 는 **조직 Owner 여도 프로젝트 멤버가 아니면** 그 프로젝트의 키 페이지가 `Not Available` 이다. 권한이 없는 게 아니라 프로젝트 스코프 문제다
  - [x] ✅ **Replicate 예비 경로를 닫았다 (8/19).** 카드가 없어 호출하면 402 였고, OpenAI 로 확정한 뒤 한 번도 부르지 않았다. **의존성 · 설정 필드 · `.env.example` 항목을 전부 걷어냈다** — 남겨 두면 「이 토큰을 구해야 하나」로 팀원 시간을 계속 먹는다. 되살릴 일이 생기면 `images/generate.py` 한 파일만 갈아 끼우면 된다 (이미지 모델 호출이 거기 한 곳뿐인 이유다)
  - [x] ✅ **`openai` SDK 를 넣지 않는다.** 쓸 엔드포인트가 둘뿐이고 `httpx` multipart 로 붙는 것을 실측했다(사진 2장 + 프롬프트가 정상 파싱되는 것까지 확인). 재시도는 `image_job.attempts` 로 우리가 관리하므로 SDK 이점이 없다
  - [x] ✅ **실제 1장 생성 — 2026-08-19.** 무신사 사진(전신 인물 + 검정 그래픽 티)으로 왕복시켰다. **프롬프트는 D3 가 만드는 그대로** 썼다 — 손으로 다듬은 문장으로 재면 스파이크가 거짓말이 된다

    | 봐야 했던 것 | 결과 |
    |---|---|
    | 얼굴 보존 | **된다.** 같은 사람으로 알아본다. 모자·헤어라인까지 유지 |
    | 무늬·색 보존 (PRD 6.3 ②) | **된다.** 검정 바탕·파란 체리·하프톤 점·초록 줄기 + 큰 글자 `Pump Up London Summerdrop` 까지 읽힌다 |
    | 걸린 시간 | **115초.** PRD 상한 10분의 1/5 다 |
    | 배경·하의·신발 | 원본 그대로 유지된다 (갈아입히기만 한다) |

    - ⚠️ **작은 글씨는 뭉갠다** — 제품컷의 `When The Summer Dropped` 가 `Drapped` 로 나왔다. 큰 글자는 정확하다. 「로고·큰 프린트는 되고 잔글씨는 안 된다」로 보면 된다
    - [x] ✅ **핏 등급이 그림을 움직인다 — 확인됨 (8/19, 3장).** 사람 사진·옷 사진·나머지 치수를 **전부 고정**하고 의류 가슴단면만 47/51/60cm 로 바꿨다. 그러면 프롬프트에서 달라지는 것이 **등급 문장 한 줄뿐**이라 차이가 나면 그 문장 때문이다

      | 의류 가슴단면 | 가슴 여유 | 판정 | 상의 실루엣 폭 (슬림 대비) |
      |---|---|---|---|
      | 47cm | 2cm | 슬림핏 | 기준 |
      | 51cm | 10cm | 레귤러핏 | **+24%** |
      | 60cm | 28cm | 오버핏 | **+52%** |

      - 오버핏은 **어깨 봉제선이 팔 위로 내려온다**(드롭숄더). 지시문에 어깨 문장이 없는데도 그렇게 나왔다 — 「오버핏」 한 단어가 실루엣 전체를 끌고 간다
      - ⚠️ **폭은 절대 픽셀로 못 잰다.** 모델이 장마다 프레이밍을 바꾼다(오버핏이 가장 멀리 잡혔다) — 첫 측정이 「오버핏이 슬림핏보다 좁다」는 엉뚱한 답을 냈다. 참조 사진에서 온 **모자 폭으로 나눈 비율**로 재야 맞고, 두 번째 기준(반바지)으로 교차 확인해도 순서가 같다
      - 결론: **리포트가 그림을 움직인다.** 사이즈를 바꿔 다시 눌러 볼 이유가 사용자에게 생긴다
    - 토큰: 입력 3009(이미지 2940 + 텍스트 69) · 출력 5488. **달러 금액은 미확인** — 사용량 대시보드가 아직 $0.00 으로 반영 전이다
    - 결론: **OpenAI 유지. Replicate 로 안 간다** (카드 등록도 필요 없어졌다)
  - ⚠️ `gpt-image-2` 는 출력 크기의 **가로·세로가 둘 다 16의 배수**여야 한다. 실제로는 `1024x1536`(세로)을 썼다
  - ⚠️ **`input_fidelity` 를 못 쓴다** — `gpt-image-2` 는 400 `invalid_input_fidelity_model` 로 거절한다. 얼굴·무늬 보존을 파라미터로 올릴 수 없고 **프롬프트에만 의존한다.** 그래도 위 결과가 나왔다
  - 호출 모양: `POST /v1/images/edits` · `image[]` 두 장(인물 먼저, 제품컷 나중) · `quality=high` · `n=1`
- [x] ✅ **D3 · 프롬프트 변환** — `images/prompt.py`. 순수 함수라 모델 없이도 다 만들고 검증했다
  - 등급 5종 매핑 완성. **기장·소매 매핑도 미리 다 넣어 뒀다** — A3 가 붙으면 이 파일은 손대지 않아도 문장이 늘어난다
  - ✅ **어깨 드롭숄더도 붙었다 (8/20 · 김정빈)** — `shoulder_diff >= 4.0` 이면 지시문이 추가된다. PRD 6.2.4 예시값을 그대로 임계값으로 채택 ([Q4](docs/open-questions.md) 종결)
  - 목록과 매핑이 어긋나면 **임포트 시점에 터진다** — 조용히 한 등급만 지시가 빠지는 걸 막는다
  - 테스트 20건. 한글이 한 글자도 안 섞이는지까지 본다
  - 계산 결과 → 영어 지시문. PRD 6.3절에 매핑표가 완성돼 있으니 그대로 옮긴다

  | 계산 결과 | 지시 |
  |---|---|
  | 가슴 여유 +18cm → 세미오버핏 | 약간 넉넉한 핏으로, 가슴에 여유가 있게 |
  | 어깨 +4cm → 드롭숄더 | 어깨 봉제선을 실제 어깨보다 아래로 |
  | 기장 → 엉덩이 중간 | 밑단이 엉덩이를 반쯤 덮게 |
  | 소매 → 손등 일부 덮음 | 소매가 손등을 일부 덮게 |

  - **실제 전달은 영어로.** 이 문장은 사용자에게 절대 노출되지 않는다
- [x] ✅ **D1 · 사진 업로드 · 저장** — `images/storage.py` · `api/photos.py`. `PUT /photos/profile` · `PUT /photos/garments/{id}`
  - **비공개 버킷 + 서명 URL(1시간).** 토큰 없는 주소는 400 이다 — 실제 Supabase 로 확인했다
  - ⚠️ **경로를 클라이언트에게서 받지 않는다.** 서버가 `{사용자id}/profile.jpg` · `{사용자id}/garment-{의류id}.jpg` 를 만든다. 받으면 남의 전신 사진을 가리키는 행을 만들 수 있고 서명 URL 이 그대로 발급된다 — `GarmentRequest` 에서 `photoPath` 를 뺀 이유
  - 사용자 폴더 **바로 아래 평평하게** 둔다. 계정 삭제가 `list` 한 번으로 다 걷어야 하고, 폴더를 더 파면 빠뜨린 가지에 전신 사진이 남는다
  - 다시 올리면 덮어쓴다. 형식이 바뀌어 경로가 갈리면 옛 파일을 지운다 (고아 방지)
  - 상한 10MB 를 **다 받기 전에** 끊는다. `await file.read()` 한 방이면 500MB 가 디스크를 지난 뒤에야 거절하게 된다
  - EXIF 가 통째로 떨어져 **촬영 위치(GPS)가 서버에 남지 않는다** (D2 의 재인코딩 덤 — 지켜야 할 성질이라 테스트를 붙였다)
  - 테스트 29건 · 돌연변이 15종 사살. 실제 Supabase 왕복 18건 통과 (업로드·서명·비공개·덮어쓰기·계정 삭제)
  - ⚠️ **계정 삭제 뒤에도 옛 서명 URL 이 잠깐 열린다** — 배포 서버 실측 `200 · cf-cache=HIT · age=6`. 객체는 이미 지워졌고 새 서명도 안 나가는데 **엣지 캐시**가 답한 것이다. 업로드 옵션으로 못 막는 것을 확인했다(기본값이 이미 `no-cache` 이고 `cache-control: "0"` 을 줘도 응답 헤더가 그대로다 — Supabase 가 붙이는 `Expires: +1h` 를 Cloudflare 가 본다). 서명 URL 은 본인에게만 나가고 토큰도 1시간이면 만료되므로 **의도적으로 여기까지만** 한다. 더 좁히려면 `SIGNED_URL_TTL` 을 줄인다
  - ⚠️ **버킷은 한 번 만들면 끝이다** — `uv run python -m images.storage`. 2026-08-19 에 `photos`(비공개)로 만들어 뒀다
  - ⏸ 히스토리·결과의 `photoUrl` 은 **D4 로 미뤘다** — 결과 이미지(`imagePath`)와 같이 붙이는 편이 낫다
- [x] ✅ **D2 · 품질 검증 — 해상도** — `images/validate.py`. 512×768 미만 거부. 테스트 22건 · 돌연변이 5종 사살
  - ⚠️ **회전을 픽셀에 굽고 나서 잰다** — 안 그러면 멀쩡한 폰 세로 사진이 768×512 로 읽혀 거부된다 ([Q7](docs/open-questions.md) 종결)
  - 의류 사진(B3)도 같은 함수를 부른다. 사람 사진 전용 판정을 넣지 않았다
  - **Pillow 를 의존성에 추가했다** (`pyproject.toml` — 공용 파일)
- [x] ✅ **D2-비전 · 전신 사진 판별** — `images/vision.py`. **자를 때의 이유가 낡아서 되살렸다** (2026-08-21)
  - 8절은 「각각 별도 비전 모델이 필요하고 6h+」라 잘랐다. 그때는 맞았지만 지금은 **이미 OpenAI 를 쓰고 있어** 호출 하나로 끝난다 — 판단이 틀렸던 게 아니라 전제가 바뀌었다
  - **막는 것**: 제품컷을 전신 사진 자리에 올리면 지금은 **2분을 기다린 끝에** 이상한 그림을 받는다. 입구에서 그 2분을 아낀다
  - 스파이크 먼저 (`scratchpad/d2_spike.py`) — `gpt-5-nano`·`gpt-5-mini` 둘 다 **4/4**. mini 가 더 빨라서(2.6~4.7s vs 4.5~7.2s) mini 로 갔다. 업로드 경로라 지연이 곧 UX 다
  - ⚠️ **애매하면 통과시킨다 (fail-open).** 업로드는 입구다 — 막히면 우회로가 없다. 응답이 **반쯤** 망가져도 판정을 통째로 포기한다
  - ⚠️ **`VISION_CHECK=false` 로 즉시 끈다.** 입구에 새 실패 모드를 놓는 일이라 스위치를 같이 만들었다
  - ⏸ **정면 판별은 여전히 안 한다** — 옆모습으로도 착용 이미지는 멀쩡히 나온다. 막지 않을 판정을 계산하면 돈과 지연만 는다
  - ⚠️ **테스트가 실제 API 를 때려서 12초 스위트가 111초가 됐다** — `conftest` 에서 autouse 로 끈다. 느린 것보다 **돈이 나가는 쪽**이 나빴다
  - 테스트 18건 · 돌연변이 **6/6 사살**. 처음엔 5/6 이었는데 살아남은 1종이 진짜 구멍이었다 — 전부 망가진 응답만 시험하고 **섞인 응답**을 안 봤다
- [x] ✅ **D4 · 비동기 잡 큐 + 상태 폴링** — `jobs/worker.py` · `images/generate.py`. 워커 5개를 앱 lifespan 에서 띄운다
  - **폴링 엔드포인트를 새로 만들지 않았다** — `GET /fittings/{id}` 가 이미 상태를 준다 (B6). 생성이 2분짜리라 5초 간격이면 충분하다
  - **사진이 둘 다 있어야 큐에 넣는다.** 없으면 「리포트만」 — 없는 것을 「대기」로 두면 영원히 안 끝나는 잡이 목록에 남는다
  - `imageUrl` · `garment.photoUrl` 을 같이 실어 보낸다 (D1 에서 미뤄 둔 것). 한 응답의 서명을 **한 번에** 받아 목록이 길어져도 왕복이 안 는다
  - ⚠️ **`SKIP LOCKED` 가 막는 것은 「중복 집기」가 아니다.** 중복은 `WHERE status='대기'` 재검사가 막는다(Postgres 가 잠금 뒤 조건을 다시 본다) — 실제 Postgres 에서 `SKIP LOCKED` 를 떼도 동시 5건 **중복 0** 이었다. `SKIP LOCKED` 가 하는 일은 **기다리지 않기**다: 다른 세션이 행을 2초 붙잡고 있을 때 **0.17초 vs 2.05초**
  - ⚠️ **SQLite 는 이 구절을 조용히 무시한다** (SQLAlchemy 가 빼고 컴파일한다). 즉 **단위 테스트는 잠금을 한 번도 안 돌려 본다** — 실제 Postgres 로 따로 쟀다 (동시 5건 · 잠금 대기 · 좀비 청소 · CASCADE, 15건 통과)
  - ⚠️ **배포 워커가 도는 순간부터 공용 DB 가 「돈이 나가는 큐」가 된다.** 로컬에서 `POST /fittings` 를 부르든 SQL 로 잡을 넣든, 그 행을 **배포된 워커가 집어가 실제로 생성한다** — 스모크·수동 테스트가 그대로 과금이다. 확인이 끝나면 잡을 「실패」로 막거나 계정을 지운다
  - 테스트 30건 · 돌연변이 18종 중 17종 사살 (1종은 **동치** — 사진 경로 가드를 빼도 `download` 에서 터져 결과가 같다)
- [x] ✅ **D5 · 타임아웃 · 실패 처리** — 상한 10분(PRD 확정값)을 `asyncio.timeout` 으로 건다. **실측은 115초라 한참 여유가 있다**
  - **실패해도 리포트는 그대로 남는다** (F-09) — `image_path` 만 비어 있다
  - 일시적 오류일 수 있으니 **한 번은 더 해 본다** (`MAX_ATTEMPTS=2`). 집어간 순간 `attempts` 를 세므로 죽어도 무한 재시도가 안 된다
  - ⚠️ **plan 원안(「생성중을 전부 실패로 민다」)을 바꿨다** — 시도가 남았으면 **큐로 되돌린다.** 재배포는 사용자 잘못이 아니라 우리 사정이고, 되돌리면 그냥 이어서 끝난다. 시도를 다 쓴 것만 실패로 굳는다
  - ⚠️ **실패 사유를 응답에 안 싣는다.** 모델 응답에 키나 내부 경로가 섞여 나올 수 있다 — 프론트가 보는 것은 「실패」라는 상태뿐이다
- [x] ✅ **D6 · 중복 생성 방지** — `api/fittings.py` 의 `_reusable()`. 재사용이면 **200**, 새로 만들면 201. 테스트 15건 · 돌연변이 6종 사살
  - **판정(리포트)이 같을 때만 같은 것으로 본다.** (사용자·의류)만 보면 프로필이 바뀌어도 옛 결과를 돌려주게 되고 사용자가 틀린 사이즈를 산다. 리포트가 곧 입력값의 지문이라 해시 컬럼을 따로 두지 않았다
  - ⚠️ **실패한 것은 재사용하지 않는다** — 돌려주면 재시도가 영영 막힌다
  - 돌고 있는 잡(대기·생성중)은 그것을 돌려준다. 같은 잡을 두 번 큐에 넣는 것도 여기서 막힌다
  - ⚠️ **자르지 말 것.** 생성 1건이 최대 10분 + 유료 API 호출이다. 시연 중 같은 조합을 두 번 누르면 그대로 비용이고, 심사위원 앞에서 10분을 기다리는 사고가 난다. 1시간짜리 작업이 사고 하나를 막는다
- [x] ✅ **E2E · 배포 서버로 한 바퀴 (8/19)** — 로컬 ASGI 가 아니라 **`https://web-production-19ef6.up.railway.app` 로** 돌렸다. 가입 → 프로필 → 사진 업로드 → `POST /fittings` → 배포 워커가 집기 → 생성 → 서명 URL 다운로드까지 **3건 전부 완료**
  - **끊긴 데 없다.** 프론트가 쓸 경로(`GET /fittings/{id}` 폴링)만으로 상태가 대기 → 생성중 → 완료로 따라온다
  - **136초 / 129초 / 131초.** 뒤 두 건은 동시에 큐에 넣었는데 **131초에 같이 끝났다** — 순차였으면 260초다. 워커 병렬이 실제로 도는 것을 배포 환경에서 확인
  - 서명 URL 로는 받아지고 **토큰을 뗀 주소는 거부**된다 — 배포 환경에서도 비공개 버킷이 지켜진다
  - ⚠️ **유료라 단계를 나눠 돌렸다.** 1장으로 경로를 확인하고 나머지 2장을 태웠다. 실패하면 워커가 한 번 더 시도하므로(`MAX_ATTEMPTS=2`) 한 번에 다 던지면 사고가 2배가 된다
  - 확인 후 **테스트 계정을 지워 공용 DB 를 0행으로 되돌렸다** (D4 의 「돈이 나가는 큐」 주의사항)
- [x] ✅ **B5 · 히스토리 API** — `GET /fittings`. 상태별 거르기 + 뱃지 개수
  - **개수는 거르기와 무관하게 전체를 센다** — 헤더 뱃지가 필터를 따라가면 안 된다
  - 목록에 의류(종류·사이즈명)를 같이 실어 보낸다. 한 줄 그리려고 의류를 다시 물어보게 하지 않는다
- [x] ✅ **B6 · 결과 조회 API** — `GET /fittings/{id}`. **만들 때와 같은 모양**이라 프론트가 렌더러를 하나만 쓴다
  - 이미지가 없어도(F-09) 생성이 실패해도 리포트는 그대로 나온다
  - ⚠️ **계약 3(잡 상태)을 여기서 확정했다** — 대기/생성중/완료/실패 + `image_job` 행이 없으면 「리포트만」

---

## 7. 타임라인

| | BE-1 · KimZion | BE-2 · 맹동훈 | BE-3 · 김정빈 |
|---|---|---|---|
| **Day 1 오전** | A1 · A2 (테스트 먼저) | **B0 세팅 → B1 스키마 즉시 공유** | **D0 스파이크 → 모델 확정** · D3 |
| **Day 1 오후** | A3 · A4 | C1 | D1 · D2 |
| **Day 2 오전** | A5 → **계약 2 공유** | C1 마무리 · `/auth/check` 넘기기 | D4 |
| **Day 2 오후** | B3 · B4 | B2 (A4 연결) | D5 · D6 |
| **Day 3 오전** | F-10 비교 | C5 · 프론트 연동 지원 | B5 · B6 |
| **Day 3 오후** | **셋이 모여 통합 · 시연 리허설** | | |

Day 3 오후는 새 기능을 만들지 않는다. 통합에서 반드시 문제가 나오고, 그때 고칠 시간이 없으면 시연이 깨진다.

✅ **백엔드 쪽 시연 리허설은 끝났다 (2026-08-20).** 데모 계정에 프로필·의류 3벌·착용 이미지
3장을 미리 만들어 뒀다 — 대본과 실측 숫자는 **[docs/demo.md](docs/demo.md)**. 전 구간을
배포 서버로 한 번 통과시켰고, 그 과정에서 **`PUT /profile` 요청 모양이 문서와 달라** 실측이
조용히 사라지는 함정을 찾아 문서를 고쳤다(프론트가 제일 먼저 걸릴 자리였다).

---

## 8. ⏸ 의도적으로 자른 것

발표에서 숨기지 말고 **"설계는 끝났고 구현은 이후"**로 말한다. PRD 8.2절이 그대로 근거가 되므로, 오히려 설계 깊이를 보여주는 재료가 된다.

| 항목 | 이유 |
|---|---|
| C2 이메일 6자리 인증 코드 | 메일 발송 인프라(SES·SendGrid) 붙이는 데 반나절. 심사위원 눈에는 "로그인이 된다" 이상으로 안 보인다 |
| C3 새 기기 감지 추가 인증 | 시연에서 발생하지 않는 경로 |
| C4 비밀번호 재설정 | 위와 같음 |
| ~~D2 비전 검증 (해상도 제외)~~ | ~~별도 모델 필요, 6h+~~ → ✅ **되살렸다 (2026-08-21).** 자른 이유가 낡았다 — 이미 OpenAI 를 쓰고 있어 호출 하나였다. **정면 판별만 여전히 자른 채다** (막지 않을 판정이라 계산할 이유가 없다) |
| F-11 생성 완료 알림 — **백엔드 발송분만** | 탭이 열려 있을 때 프론트가 `Notification` 을 띄우는 건 백엔드 0h라 살린다. 탭을 닫아도 오는 Web Push(서비스워커·VAPID·구독 테이블)는 **3h+** 라 자른다 — 그 3h면 F-10이나 D6가 죽는다. 닫으면 히스토리(B5)에서 확인한다. ⚠️ 팀 확인 대기 ([Q6](docs/open-questions.md)) |
| 마이그레이션 도구 (Alembic) | 3일간 3명이 마이그레이션 순서를 맞추는 비용이 이득보다 크다. 공용 DB + 손 `ALTER` 로 간다 |
| E1 보관기간 자동삭제 (30일·90일) | 배치 잡이라 3일 시연 중 한 번도 안 돈다 → **정책 문서로 제출** |
| E2 이상 로그인 알림 | 위와 같음 |
| E3 1년 미접속 계정 삭제 | 위와 같음 |

---

## 9. ⚠️ 리스크

| # | 리스크 | 대응 |
|---|---|---|
| R1 | **이미지 생성 모델 미정** (PRD Q1) — "후보 2~3개 실측 비교"할 시간이 없다 | D0으로 Day 1 오전에 끝낸다. Replicate로 감싸두면 교체가 문자열 하나 |
| R2 | ~~10분 비동기 작업이 스택 선택을 제약~~ **→ 해소.** Railway 컨테이너 + Postgres 큐로 결정 | 서버리스로 갈아타지 않는다. 갈아타면 D4가 통째로 무효가 된다 |
| R3 | **B1 스키마 지연이 전체를 막는다** | 공용 DB라 적용 즉시 셋 다 본다. 완벽 대신 속도 — 2시간 내 초안 |
| R4 | **공용 DB를 BE-2가 깨면 셋이 같이 멈춘다** | R3와 맞바꾼 대가다. 스키마 변경은 **BE-2만**, 변경 전 팀에 알린다 |
| R5 | **클로드 3개가 같은 파일을 건드린다** | [CLAUDE.md 3절](CLAUDE.md) 디렉터리 소유권. 커밋 전 `git diff --stat`으로 내 영역 밖이 안 바뀌었는지 확인 |
| R6 | **클로드가 확정 상수를 지어낸다** | [CLAUDE.md 1절](CLAUDE.md)이 매 세션 자동으로 읽힌다. 특히 임계값과 기장 문구 |
| R7 | 총량 28h를 3일에 넣는 계획이라 여유가 적다 | 막히면 8절로 더 내린다. **A그룹과 F-10은 마지막까지 지킨다** — 이게 없으면 서비스가 아니라 그냥 이미지 생성기다 |

---

## 10. 작업 규칙

브랜치·커밋·리뷰 규칙과 하지 말 것 목록은 [CLAUDE.md](CLAUDE.md)에 있다.

이 문서에만 해당하는 것:

- 작업을 시작하면 해당 체크박스를 🔜로, 끝나면 ✅로 바꾼다
- 범위를 미룰 때는 지우지 말고 ⏸로 바꾸고 **왜 지금 안 하는지**를 남긴다
